" " ", "; "; "cann't "create "ex1.7.cpp"; "output #include #include #include #include #include &w (sorted.txt) -1; 0; 1; : << << << << << << << << << << << <algorithm> <fstream> <iostream> <string> <vector> = >> cerr cerr const cout cout done!" endl; endl; endl; failed!" file file file for(auto if(!in_file) if(!outFile) ifstream in_file(file); in_file.close(); int main() namespace ofstream outFile outFile outFile("sorted.txt"); outFile.close(); outFile.flush(); read return return return sort(words.begin(), std; string string to using vector<string> w while(in_file word word) word; words) words.end()); words.push_back(word); words; { { { { } } } } 